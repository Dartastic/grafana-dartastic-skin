# Regenerated from the upstream image on every build-and-push.sh run.
# These files are NOT checked in — keep this dir empty in git history.
upstream-en-US.json
skin-en-US.json
# Same, for build-grafana-skinned.sh (standalone Grafana locale extract).
upstream-grafana-en-US.json
skin-grafana-en-US.json
customer-dashboards/
reference-demo-dashboards/
# Staged ai-gateway Dart source (rsynced from ../ai-gateway/) for
# the Dockerfile's stage-1 `dart compile exe`.  Regenerated each
# build; the source-of-truth lives at hosted/ai-gateway/.
ai-gateway/
# Staged AI Grafana plugin source (rsynced from ../grafana-plugins/
# dartastic-ai-panel/) for the Dockerfile's npm build stage. Same deal as
# ai-gateway/ above — this one was tracked by mistake when the plugin landed,
# so the committed copy went stale the moment the real source moved, and every
# build dirtied the tree re-staging it. Source of truth is
# hosted/grafana-plugins/dartastic-ai-panel/.
ai-plugin/
