{
  "_comment": [
    "hosted#194 expected-fail (xfail) list — findings the scanner CORRECTLY",
    "detects on the current image that are tracked in open issues. Each lets",
    "the gate stay green while the referenced issue is open, WITHOUT hiding",
    "the finding (it's reported as KNOWN-FAILURE in every run).",
    "Enforcement flip: check-expected-fail-freshness.sh fails the build when",
    "an entry's issue is CLOSED — the fixing PR must delete the entry (paper",
    "trail). If a check unexpectedly PASSES while its entry is still here,",
    "scan.mjs prints an XPASS warning telling you to remove the entry.",
    "Schema: id (stable name), issue (hosted issue number), kind",
    "(text|attr|title|icon), pattern (case-sensitive JS regex against the",
    "finding snippet), routes (prefixes or [\"*\"]), note.",
    "",
    "EMPTY as of hosted#196/#187: the skin's attribute pass + in-sentence mark",
    "rewrite + flame-icon neutralisation (dartastic-skin.js) resolved every",
    "previously-tracked leak on the lgtm-skinned image. The gate now passes",
    "with zero xfails — any new entry means a genuinely new leak to fix, not",
    "an accepted one. (The standalone grafana-skinned Cloud image may add its",
    "own entries; those live/merge with that build.)"
  ],
  "entries": []
}
